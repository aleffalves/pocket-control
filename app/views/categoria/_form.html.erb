<%= form_with(model: categorium) do |form| %>
  <% if categorium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(categorium.errors.count, "error") %> prohibited this categorium from being saved:</h2>

      <ul>
        <% categorium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, class: "form-label mb-2", style: "display: block" %>
    <%= form.text_field :nome, class: "form-control mb-3" %>
  </div>

  <div>
    <%= form.submit texto_da_acao, class: "btn btn-primary" %>
    <%= link_to "Voltar", categoria_path, class: "btn btn-secondary" %>
  </div>
<% end %>
